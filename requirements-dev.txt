# Fallback only if you cannot use `uv` (prefer: `uv sync` + `uv run …`).
# From repo root: pip install -r requirements-dev.txt
-e packages/fixops-contract
-e services/controller
-e services/worker-obs
-e services/worker-k8s
-e services/executor
-e services/mcp-fixops-obs
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
respx>=0.21
