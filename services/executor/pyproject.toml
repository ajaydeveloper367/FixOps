[project]
name = "fixops-executor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "pydantic>=2.6"]

[project.scripts]
fixops-executor = "fixops_executor.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
