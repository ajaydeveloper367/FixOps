[project]
name = "mcp-fixops-obs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcp>=1.2", "httpx>=0.27"]

[project.scripts]
mcp-fixops-obs = "mcp_fixops_obs.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
